---
import { Icon } from 'astro-icon/components';
import { Image } from 'astro:assets';
import {
    Container,
    TextCardSection,
} from '@components/odyssey-theme';
import Layout from '../../layouts/Page.astro';

const seo = {
    title: 'Neuroscience Insights | SCI-NAD',
    description: 'Explore insights from our gamified neuroscience learning platform.',
};

const metrics = [
    { icon: 'mdi:brain', label: 'Neural Pathways Explored' },
    { icon: 'mdi:clock-fast', label: 'Learning Speed Increase' },
    { icon: 'mdi:account-group', label: 'Active Users' },
    { icon: 'mdi:chart-line', label: 'Knowledge Retention Rate' },
];

---

<Layout {seo}>
    <Container>
        <TextCardSection>
            <h1>Neuroscience Learning Insights</h1>
            <p>Discover the impact of SCI-NAD's gamified learning approach on neuroscience education.</p>

            <div class="metrics-grid">
                {metrics.map(metric => (
                    <div class="metric-card">
                        <Icon name={metric.icon} class="w-16 h-16 text-primary-500" />
                        <h3>{metric.label}</h3>
                        <div class="blur-box"></div>
                    </div>
                ))}
            </div>

            <h2>Key Findings</h2>
            <ul>
                <li><strong>Engagement:</strong> Users spend significantly more time per session compared to traditional learning methods.</li>
                <li><strong>Skill Improvement:</strong> The majority of users report improved understanding of complex neural concepts within weeks.</li>
                <li><strong>Problem-Solving:</strong> Users show a substantial increase in ability to solve complex neuroscience problems after using SCI-NAD.</li>
                <li><strong>Collaboration:</strong> Inter-disciplinary collaboration among users has notably increased through our platform.</li>
            </ul>

           

            <p>These insights demonstrate the effectiveness of SCI-NAD's gamified approach in enhancing neuroscience education and research capabilities.</p>
        </TextCardSection>
    </Container>
</Layout>

<style>
    .metrics-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 2rem;
        margin: 3rem 0;
    }
    .metric-card {
        background-color: var(--theme-surface-1);
        padding: 2rem;
        border-radius: 12px;
        text-align: center;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
    }
    .metric-card:hover {
        transform: translateY(-5px);
    }
    .blur-box {
        margin-top: 1rem;
        height: 30px;
        background-color: var(--theme-primary);
        filter: blur(10px);
        opacity: 0.7;
        border-radius: 15px;
    }
    .feature-image {
        margin: 3rem 0;
        text-align: center;
    }
    .feature-image img {
        max-width: 100%;
        height: auto;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    }
    ul {
        margin-left: 1.5rem;
        margin-bottom: 1.5rem;
    }
    h2 {
        margin-top: 3rem;
        margin-bottom: 1.5rem;
    }
</style>