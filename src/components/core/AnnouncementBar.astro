---
const {text, href, newTab} = Astro.props;
---

<!-- 
To use this AnnouncementBar component:

1. Import it in your Astro file:
   import AnnouncementBar from '../components/core/AnnouncementBar.astro';

2. Use it in your markup, passing the required props:
   <AnnouncementBar 
     text="Your announcement text here" 
     href="/your-link-here" 
     newTab={true} 
   />

3. Customize the props as needed:
   - text: The content of your announcement
   - href: The URL the announcement links to
   - newTab: Set to true if you want the link to open in a new tab

Example:
<AnnouncementBar 
  text="Check out our new feature!" 
  href="/new-feature" 
  newTab={false} 
/>

This will create an announcement bar at the top of your page with the specified text and link.
-->

<div class="announcement-banner__container">
  <p>
    {newTab ?
    <a href={href} target="_blank" rel="noopener noreferrer">
      {text}
    </a> :
    <a href={href}>
      {text}
    </a>}
  </p>
</div>

<style>
.announcement-banner__container {
  padding: 0.5rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: var(--theme-primary);
  color: var(--theme-on-primary);
  position: relative;
  z-index: 100;
}
.announcement-banner__container p {
  margin: 0 auto;
}
</style>